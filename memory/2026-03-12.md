# 2026-03-12

## Prometheus exporter 추가 — commit `bd7fcbe`
- consumer.py에 prometheus_client 메트릭 추가
- Counter: events_processed_total, events_skipped_delete_total, events_errors_total
- Gauge: pending_total, last_run_timestamp, last_run_backed_up, last_run_errors, last_run_duration_seconds
- main.py에서 start_http_server (기본 포트 9101, config.toml [metrics] port)
- backup.py: 실행 결과를 Redis `backup:last_run` Hash에 기록
- requirements.txt에 prometheus_client>=0.20.0 추가

## 로그 레벨 설정 — commit `377c502`
- config.toml `[logging] level = "INFO"` (DEBUG/INFO/WARNING/ERROR)
- main.py, backup.py, prune.py에 적용 (force=True)

## Kafka 파티션 증설
- topic `file-tracker-events` 파티션 1 → 10 증설
- 파티션 키 = hostname → 노드별 파티션 분산
- 코드 변경 없음 (Kafka 서버 설정)

## 멀티 consumer 스케일링 테스트
- consumer 2대 동시 실행 (같은 group_id, 다른 metrics port)
- Kafka 자동 리밸런싱 확인: 10개 파티션이 2대에 분산
- 전체 LAG 0, Redis pending 정상 집계

## 최종 코드 리뷰 — commit `ec61c65`
5건 버그/개선 수정:
1. cli.py status -v: json.loads → 단순 문자열 처리 (pending value가 문자열로 변경된 것 반영)
2. backup.py: `import time as _time` 중복 제거
3. main.py: gauge 갱신을 시간 기반(10초)으로 변경 (이벤트 폭주 시에도 갱신 보장)
4. cli.py repo_url: use_tls 미반영 버그 수정
5. RepoManager: _pending set으로 동일 repo 중복 init 방지

## 프로덕션 전 추가 제안 (미구현)
- Redis 연결 재시도 로직 (권장)
- Kafka 연결 실패 핸들링 (권장)
- healthcheck endpoint (환경에 따라)
- E2E 테스트 스크립트 (권장)

## 커밋 이력
- `bd7fcbe` — Prometheus exporter
- `377c502` — 로그 레벨 설정
- `ec61c65` — 최종 코드 리뷰 5건 수정
