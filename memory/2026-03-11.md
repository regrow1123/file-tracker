# 2026-03-11 메모리

## SIGHUP hot reload → 롤백
- config hot reload (SIGHUP) 구현 후 "실질적으로 불필요" 판단하여 revert
- `systemctl restart`로 충분. 코드 복잡도만 증가
- commit `305be71` → revert `a86b5b0`

## fentry 자동감지 → 불필요 판단
- kprobe → fentry 전환 시 성능 개선 미미 (CPU 0.24% → ~0.20%)
- BPF inode dedup이 이미 99.99% 이벤트 제거하므로 kprobe 오버헤드가 병목 아님
- 구현 안 함

## log rotation → 불필요
- journald(`StandardOutput=journal`)로 출력, journald가 자체 크기 관리
- 별도 log rotation 불필요

## README 작성 완료
- commit `2990d9e` — 아키텍처, 빌드/설치/설정, 성능, 트러블슈팅

## 증분 스냅샷 백업 시스템 설계 결정

### 아키텍처
- 노드(file-tracker) → Kafka → 소비자(backup-consumer) → restic → MinIO(S3)
- Kafka = 디커플링 버퍼. 소비자 추가만으로 용도 확장 가능
- DB 직접 넣기 X → 수백 노드 동시 write 시 DB 병목

### 변경 DB
- Kafka 이벤트를 변경 DB에 upsert → 중복 제거 (같은 파일 100번 수정 → 1건만 백업)
- DB 선택: Redis (소비자 여러 대 가능) 또는 SQLite (단일 소비자)
- 키: `(hostname, path)`, 값: 최신 이벤트 타입

### 백업 도구: restic
- BSD 2-Clause, 상업적 사용 무료
- 청크 기반 dedup → 1GB 파일 10버전 ≈ 1GB+α (S3 versioning은 10GB)
- S3 네이티브 지원 (`restic -r s3:http://minio:9000/bucket`)
- `--files-from` 으로 변경 파일만 선택 백업
- 대안 검토: borg(SSH만, S3 X), kopia(OK, 병렬 강력), duplicacy(상용 유료)

### 스토리지: MinIO
- S3 호환 오브젝트 스토리지 (자체 구축)
- restic 백엔드로 사용
- S3 versioning은 전체 복사본이라 비효율 → restic 청크 dedup 사용

### 원본 파일시스템: Lustre
- 수PB 규모, 수백억 파일

### Kafka 개념
- offset commit = "여기까지 처리 완료" 마커. 소비자 재시작 시 이어서 읽기
- 메시지 소비해도 삭제 안 됨. retention 설정(기본 7일)에 따라 자동 삭제
- 같은 메시지를 여러 소비자가 독립적으로 읽기 가능

## PRD/TDD 업데이트
- commit `e0552e8`
- 소비자(backup-consumer) 설계 추가
- 7단계 구현 계획: B-1(Kafka+Redis) → B-2(restic+MinIO) → B-3(멀티노드) → B-4(스케줄러) → B-5(풀스캔 보정) → B-6(CLI) → B-7(운영)
- 소비자 언어: Python 추천 (I/O 바운드)
- 총 예상 5~6일

## kopia → restic 최종 결정
- kopia: 동시 쓰기 가능하지만 `--file-list` 옵션 없음. staging dir + symlink 우회 필요 → hacky
- restic: `--files-from` 네이티브 지원. 동시 쓰기 제한은 depth 기반 repo 분리로 해결
- commit `69267f8`

## Redis 구조 최종: 단일 Hash
- `pending` Hash 하나. HSET upsert, HGETALL batch, HDEL cleanup
- String+Set 이중 구조 → Hash 단일로 단순화 — commit `e935774`

## Lustre 공유 마운트 영향
- 모든 노드 + 소비자가 동일 `/home` 마운트
- hostname 무관하게 경로만으로 dedup (Redis 키 = path만)
- SSH pull 불필요 — 소비자가 Lustre 직접 접근

## timestamp 비교 로직
- Redis upsert 시 기존 ts보다 오래된 이벤트 무시 (순서 역전 방지)
- Lustre 환경에서 여러 노드의 이벤트가 다른 Kafka 파티션 거쳐 순서 뒤바뀔 수 있음
- commit `f3d0885`

## backup-consumer 인프라 구축
- docker-compose에 Redis(7-alpine, AOF), MinIO(latest, 9000/9001) 추가
- restic 0.16.4, mc CLI 설치
- MinIO 버킷 `file-tracker-backup` 생성
- commit `2da19f8`

## B-1: Kafka consumer + Redis (완료)
- `consumer/consumer.py`: confluent-kafka → Redis pending Hash
- timestamp 비교로 순서 역전 방지 검증 (skipped_old 카운트)
- rename: HDEL old + HSET new
- commit `4e7a828`

## B-2/B-3: restic + MinIO + repo 분리 + 병렬 (완료)
- `consumer/backup.py`: Redis pending → repo별 그룹핑 → ThreadPoolExecutor → restic backup
- `get_repo_id(path, base, depth)`: 경로에서 repo_id 추출
- `ensure_repo()`: 첫 이벤트 시 restic init (initialized set 캐시)
- `--files-from`으로 변경 파일만 백업, delete는 스킵
- 백업 완료 후 HDEL, repo 매핑 안 되는 경로도 HDEL
- commit `1218a4d`

## B-4: 통합 데몬 + 스케줄러 + prune (완료)
- `consumer/main.py`: consumer + backup + prune 통합
- Scheduler: croniter 기반, 30초 간격 체크
- 증분 백업: `[backup] schedule` (기본 매일 03:00)
- 스냅샷 정리: `[prune] schedule` + `keep_days` (기본 매주 일 04:00, 90일 보존)
- `prune_all_repos()`: mc ls로 repo 자동 탐색 → restic forget --keep-within
- SIGINT/SIGTERM graceful shutdown
- commit `904716c`

## 현재 상태
- file-tracker 에이전트: **완료**
- backup-consumer: **B-4까지 완료** (B-5 풀스캔 보정 다음)
- 최신 commit: `b561a72`
- 남은 단계: B-5(풀스캔), B-6(CLI), B-7(운영)
